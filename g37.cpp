/*int k = oldsize;
  for(int i =0;i < oldsize ;i++){
    size_t it = k +i;
    int j = (newsize - 1) - k -i;
    //at(k+i) = at(j);
    iterator itinsert = begin() + j;
    T value = *itinsert;
    insert_by_pos(it, value);*/
